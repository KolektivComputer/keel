---
title: Client Setup
description: Point the @kolektiv npm scope at Nexus releases and install the router.
category: getting-started
order: 3
---

import CodePair from "../../../components/CodePair.astro"
import { path } from "../../../lib/paths"

Svelte is the first adapter. Client packages live in the hosted `keel-npm`
repository on Nexus at `repo.yuri.capital`. Put this in `.npmrc` at the pack
root (or your user `~/.npmrc`):

```bash
# .npmrc
@kolektiv:registry=https://repo.yuri.capital/repository/keel-npm/
```

Resolving does not require Nexus credentials; publishing does.

## Releases (recommended)

Current release: `0.0.1`. Then install:

<CodePair
  title="package.json — Svelte pack"
  fw="svelte"
  ts={`{
  "dependencies": {
    "@kolektiv/keel": "0.0.1",
    "@kolektiv/keel-svelte": "0.0.1",
    "svelte": "^5.16.0"
  },
  "devDependencies": {
    "@kolektiv/keel-pack": "0.0.1",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "vite": "^6.2.0"
  }
}`}
  js={`{
  "dependencies": {
    "@kolektiv/keel": "0.0.1",
    "@kolektiv/keel-svelte": "0.0.1",
    "svelte": "^5.16.0"
  },
  "devDependencies": {
    "@kolektiv/keel-pack": "0.0.1",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "vite": "^6.2.0"
  }
}`}
/>

`@kolektiv/keel` is the host router (visits, history, prefetch, `action()`).
`@kolektiv/keel-svelte` is the Svelte 5 bindings. `@kolektiv/keel-pack` is
the toolchain: `keel-scaffold` from a live host schema, a Vite plugin
(`keelPack`) that discovers pages and writes a `.feb`, and `keel-pack` to
zip a dist by hand.

## Pre-releases

Current pre-release: `0.0.2-SNAPSHOT.1`. The `-SNAPSHOT.1` suffix is part of
a literal, unique version, and the same `keel-npm` registry serves it. Swap
the `0.0.1` pins above for `0.0.2-SNAPSHOT.1` to track the pre-release line.

Next: <a href={path("/docs/getting-started/pack")}>scaffold a pack</a>.
The host half is
<a href={path("/docs/getting-started/host")}>Host (Ktor)</a>.
